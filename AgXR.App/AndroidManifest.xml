<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- ARCore requires camera perms, but we list it above too -->
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.camera.ar" android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true">
        
        <!-- ARCore Metadata for Geospatial -->
        <meta-data android:name="com.google.ar.core" android:value="required" />
        <meta-data android:name="com.google.android.ar.API_KEY" android:value="AIzaSyD9eOKyjDZ_hq-p0qxRtXFMSv5r3nwjzS0" />

        <!-- Activity declaration is handled by .NET Android build system via Activity attribute in C# -->
        <activity android:name=".SavedTagsActivity"
            android:label="Saved Observations"
            android:parentActivityName=".MainActivity" />
    </application>
</manifest>
